#include <stdio.h>
#include <fstream>
#include <string>

using namespace std;

bool subStrValidate(string line){
    if(line.substr())
}


int readData(string filename){
    ifstream file;
    string line = "";
    file.open(filename);
    while(getline(file, line)){
        if(line.length() > 4 && ){
            
        }
    }

}

void outputFile(Arr){
    
    for (int i = 0; i < 13; i++) 
    { 
        for (int j = 0; j < 13 ; j++) 
        { 
            Arr[i][j]
        } 
    } 
  
    return 0; 
} 

}